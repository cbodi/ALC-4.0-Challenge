<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.zealtechconsulting.alc4phase1challenge">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- This line helps to grant internet access to our WebView on activity_about_alc.xml-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyProfile"
            android:label="@string/my_profile"> <!-- This line helps to change the title of this activity -->

        </activity>

        <activity
            android:name=".AboutALC"
            android:label="@string/about_alc"> <!-- This line helps to change the title of this activity -->

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>